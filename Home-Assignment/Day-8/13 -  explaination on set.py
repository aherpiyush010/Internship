# 🔹 Part E: Tuple vs Set – Conceptual Task
# 13.	Explain (in comments or writing):
# •	When to use a tuple over a list or set
# •	When to use a set over a list or tuple

# 🔹 When to use a TUPLE over a LIST or SET:

# • Use a tuple when your data should NOT change (i.e., it's immutable).
# • Tuples are ideal for fixed collections like coordinates (x, y), RGB colors, or database records.
# • Tuples can be used as keys in dictionaries because they are hashable (unlike lists and sets).
# • Tuples preserve order and allow duplicate values.
# • Use a tuple when you want a lightweight, fixed-size container.


# 🔹 When to use a SET over a LIST or TUPLE:

# • Use a set when you need to store UNIQUE elements only.
# • Sets are unordered and do not allow duplicates.
# • Sets are useful for fast membership testing (checking if an item exists).
# • Sets are good for mathematical operations like union, intersection, and difference.
# • Sets are more efficient than lists for removing duplicates.

